# Example environment variables for Docker Compose
# Copy this to .env in the same directory as docker-compose.yml
# and customize the values

# Database
POSTGRES_PASSWORD=your_secure_password_here

# Minibot configuration (adjust based on actual requirements)
# MINIBOT_API_KEY=your_api_key_here
# MINIBOT_LOG_LEVEL=info
# ANTHROPIC_API_KEY=your_anthropic_key_here
