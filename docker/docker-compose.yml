version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: minibot-postgres
    environment:
      POSTGRES_DB: minibot
      POSTGRES_USER: minibot
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    volumes:
      - ../data/postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - minibot-net
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: minibot-redis
    command: redis-server --appendonly yes
    volumes:
      - ../data/redis:/data
    ports:
      - "6379:6379"
    networks:
      - minibot-net
    restart: unless-stopped

  # Agent orchestrator (placeholder - adjust based on actual requirements)
  # Uncomment and configure once you have the actual images/builds
  # orchestrator:
  #   build:
  #     context: ../
  #     dockerfile: docker/Dockerfiles/orchestrator.Dockerfile
  #   container_name: minibot-orchestrator
  #   depends_on:
  #     - postgres
  #     - redis
  #   volumes:
  #     - ../config:/app/config:ro
  #     - ../data/logs:/app/logs
  #   environment:
  #     - DATABASE_URL=postgresql://minibot:${POSTGRES_PASSWORD:-changeme}@postgres:5432/minibot
  #     - REDIS_URL=redis://redis:6379
  #   ports:
  #     - "8080:8080"
  #   networks:
  #     - minibot-net
  #   restart: unless-stopped

networks:
  minibot-net:
    driver: bridge
